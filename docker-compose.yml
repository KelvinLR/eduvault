version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: eduvault-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: eduvault
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
